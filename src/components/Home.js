import Addnote from './Addnote';
import Notes from './Notes';

export const Home = (props) => {
  
    return (
        <div>
            <Addnote/>
            <Notes />
        </div>
    )
}

export default Home;